- name: installation of database 
  hosts: db
  become: yes
  tasks:
  - name: installing mysql-server 
    ansible.builtin.dnf:
     name: mysql-server
     state: latest
  - name: start sql server
    ansible.builtin.service:
      name: mysql
      enabled: yes
      start: started 